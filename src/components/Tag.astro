---
interface Props { class?: string; }
const { class: className = '' } = Astro.props;
---

<span class={`inline-flex rounded-full border-2 border-brand-border bg-white px-4 py-1.5 text-sm font-medium text-brand-ink ${className}`}>
  <slot />
</span>
